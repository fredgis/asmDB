{
  "name": "asmdb-mcp",
  "version": "1.1.0",
  "description": "Model Context Protocol server exposing the asmdb transactional engine as a generic CRUD store (agent memory is one example use case).",
  "type": "module",
  "bin": {
    "asmdb-mcp": "src/server.js"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "license": "MIT"
}
