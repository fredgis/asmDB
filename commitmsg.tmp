Show the workload in both themes, and the states it now derives

The lineage capture is refreshed: its two edges are coloured differently
because the state is now computed from real run history rather than from
a value written once at creation and never revisited.

A second capture shows the Monitoring tab, which is where that work is
visible. Three runs read back from Fabric, and each link's state paired
with the sentence that justifies it - one Active because it is scheduled
and its last run did not fail, one Planned because its notebook exists
but nothing runs it yet. The words mean something now, so the README
should show them meaning it.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
Copilot-Session: ab8ae5e0-e62f-4539-823d-70570d7062e3
